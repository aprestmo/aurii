{
  "name": "aurii",
  "version": "0.1.0",
  "private": true,
  "description": "Aurii — Declarative Runtime for Structured Knowledge",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tests/fixtures/external-product"
  ],
  "dependencies": {
    "yaml": "^2.7.1"
  },
  "scripts": {
    "build": "bun run --filter='*' build",
    "test": "bun run --filter='@aurii/validation' test && bun run --filter='@aurii/types' test && bun run --filter='@aurii/db' test && bun run --filter='@aurii/core' test && bun run --filter='@aurii/api' test && bun run --filter='@aurii/sdk' test && bun run --filter='@aurii/studio' test && bun run --filter='@aurii/studio-app' test && bun test tests/architecture tests/pack",
    "pack:packages": "bun run scripts/pack-public-packages.ts",
    "lint": "bun run --filter='*' lint",
    "typecheck": "bun run --filter='*' typecheck",
    "dev": "bun run --filter='@aurii/api' dev & bun run --filter='@aurii/studio-app' dev",
    "studio": "bun run --filter='@aurii/studio-app' dev",
    "studio:build": "bun run --filter='@aurii/studio-app' build",
    "serve": "bun run --filter='@aurii/api' serve",
    "db:migrate": "bun run --filter='@aurii/db' migrate",
    "db:seed": "bun run --filter='@aurii/db' seed",
    "ops:persistence-proof": "bun run scripts/ops-persistence-proof.ts"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
