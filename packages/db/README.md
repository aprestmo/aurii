# @aurii/db

Drizzle schema, migrations, and seed helpers for Aurii Core storage.

This is a **runtime/storage** package. Product frontends must not import it. Project bootstrap tooling may depend on it transitively through `@aurii/core`.

See [`docs/PACKAGES.md`](../../docs/PACKAGES.md).
