# @aurii/types

Shared domain types for Aurii packages (projects, datasets, published routes, Studio config).

Consumed by `@aurii/core`, `@aurii/validation`, `@aurii/studio`, and `@aurii/db`. External product UIs should prefer `@aurii/sdk` types.

See [`docs/PACKAGES.md`](../../docs/PACKAGES.md).
